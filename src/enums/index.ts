/*eslint-disable no-unused-vars*/
export enum USER_ROLE {
	ADMIN = "admin",
	EMPLOYER = "employer",
	CANDIDATE = "candidate",
}

export enum IMAGE_TYPE {
	JPG = "jpg",
	JPEG = "jpeg",
	PNG = "png",
}

export enum ResponseStatus {
	SUCCESS = "success",
	FAILED = "failed",
}
